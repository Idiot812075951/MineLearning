# Guren Q skill UE demo — 2026-09-13

User authorizes exporting current Blender Q_Dash and Q_GrabLift (head look-at/radiation version) to the existing Guren skeleton, and implementing the full single-player Q prototype in L_Guren_Retarget_Test. Reference: user-supplied UE Q checklist. Reuse /Game/MineLearning/VFX/RadiantDissolve/BP_RadiantDissolve_Test public Dissolve(Target, OriginWS) API, materials and Niagara. No new dissolve shader, damage system, new PlayerController, multiplayer or architecture migration.

Source of truth: live V027A_R16_QGrabHeadIK.blend including current unsaved user edits. Current Q_Dash Action is F1–18 (updated since earlier R13); sample the actual current actions and enabled control constraints. Q_GrabLift F1–123 at 30 FPS. Export-only rig copy, original 59-bone rest/hierarchy and source Actions protected. Dense evaluated baking permitted only on export copy to capture IK.

UE implementation: nearest demo target within 500, <=200 direct grab, otherwise root-motion dash with Motion Warping. Original Q authoring poses remain unchanged. Use editable dummy stand and head-contact references, montage events for transitions/attachment/dissolve/end, safe cancellation. Q uses a full-body slot after existing locomotion/attack layering. Existing flight/attack behavior protected; Q blocks flight and normal attack while active. Existing test map receives dummy and debug access only. Verify imports, Blueprint/C++ compile, multiple ranges, interrupts and cleanup in PIE. Report actual limitations.

## Current revision: impact and long dash

User requests stronger camera impact (brief shake, slow motion, close-up) and a much longer dash with restrained afterimages. Use existing UE animations and skeleton as protected source; no Blender reauthoring. Max selection distance becomes 2000 cm as an adjustable game scale (roughly 4–8 mecha body widths for the requested 500–1000 game units, not real-world yards). Retain 200 cm direct grab threshold. Adapt dash montage duration to distance. Presentation component owns temporary camera offsets, real-time-bounded slowdown and fading magenta pose afterimages; all clean up on cancellation/end. Add long-distance targets in the same test map. Verify long range endpoints, near grab, impact timing and full restoration including interruptions.

Latest correction: shake ONLY at StartDissolve, not contact or dissolve completion. On GrabContact smoothly orbit the existing player spring-arm camera to Guren's front, slightly offset for clear victim/head composition. Preserve/restore previous view, boom length, offset and input lock on completion/cancellation/EndPlay. Existing contact slow motion retained. Expose execution-camera tuning and document all Q setup/defaults, including parameters that remain C++ constants.
